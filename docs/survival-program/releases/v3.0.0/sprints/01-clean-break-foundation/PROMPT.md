# Sprint 01 prompt — clean-break foundation

Execute only `DNP1-GOV` and then `DNP1-INV` from this release. Preserve all
reviewed Deep-native work and existing dirty/residual files. Do not delete
production code during this sprint.

The sprint ends with an active reproducible program release, a reviewed exact
dependency inventory and red/static gate specifications. It does not end with
a renamed protocol or a compatibility adapter.

